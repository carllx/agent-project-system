# Change Packet 模板

_用途：IDE Agent 在执行完 RR Lead 的 `NEXT_WORK_ORDER` 后，向 RR Lead 返回执行结果与本地事实证据。_

---

## 基本信息

- **当前循环轮数**: [当前是第 N 轮，如果 N=6，请强制提醒 RR Lead 考虑 Handoff]
- **执行耗时**: [近似耗时]

## 变更摘要 (Git Diff)

```diff
[放置执行完指令后的核心 Git Diff 输出。如果太长，只保留核心改动逻辑，其余一笔带过]
```

## 测试与验证结果

- **执行的验证命令**: `[填入具体的测试或运行命令]`
- **命令退出码**: [0 代表成功，非 0 代表失败]

**输出证据 (Output):**
```text
[填入真实运行的日志、控制台输出或报错信息]
```

## IDE 事实纠偏 (如有)

> [如果 RR Lead 此前的判断与本地发生冲突，IDE 在此利用上述输出证据明确指出错误，并要求 RR Lead 重新提供思路。如果没有冲突，请填 None。]
