# IDE Agent 指南

本文档是 IDE Agent 在本项目中的核心准则与边界声明。

## 阅读顺序

当 IDE Agent 接入本项目时，请按照以下顺序加载上下文：

1. 本文件 (`AGENTS.md`)。
2. `docs/PROJECT_CHARTER.md`。
3. `docs/ARCHITECTURE.md`。
4. `state/active-work-item.md` (读取当前任务进度与事实状态)。
5. 相关的 Workflow 和 Skill 文件 (按需)。

## 规则优先级（强制）

在发生冲突或不确定时，IDE Agent 必须严格遵守以下优先级（由高到低）：

1. **用户当前明确作出的目标、决定和授权** (最高优先级，可覆盖一切)。
2. 本文件 (`AGENTS.md`) 及其指向的项目宪章 (`docs/PROJECT_CHARTER.md`)。
3. 当前适用的 Workflow 和 Skill 定义。
4. 当前 Work Item 的目标与完成标准。
5. `state` 文件夹中记录的当前事实。
6. Agent 自己的建议、未验证推测或外部经验。

### 规则释义

* **事实不覆盖规则**：`state` 文件只记录发生过的事实，绝对不能用于覆盖上述系统规则。
* **流程不覆盖决断**：Skill 文件只定义操作流程，绝对不能用于推翻用户当前决定。
* **事实纠正推测**：IDE Agent 拥有并必须使用本地项目的真实运行结果、测试报告和文件内容（即“本地证据”），来**纠正** RR Lead 可能产生的误解或凭空臆断。
* **本地大于外部**：RR Lead 通过外部调研得出的结论，如果与本地真实证据冲突，**不能**覆盖本地事实，必须以本地代码运行结果为准。

## 触发规则与硬性边界

* 必须优先阅读 `active-work-item.md` 确认是否存在进行中的任务，并遵守单任务限制。
* 严禁扫描、读取、上传或修改超出本项目目录范围的用户个人文件。
* 严禁尝试读取 Cookie、Token、密钥或任何平台账号凭据。
* 所有与用户的交互，涉及决策时必须使用包含“三个白话选项加一个推荐”的格式。
